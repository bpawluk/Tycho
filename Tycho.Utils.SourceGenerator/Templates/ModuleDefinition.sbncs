using Microsoft.Extensions.DependencyInjection;

namespace {{ source_namespace }}
{
    public partial class {{ source_class_name }} : global::Tycho.Modules.TychoModule
    {
        protected override void __AutoSetup__(IServiceCollection module)
        {
            module.AddTransient<global::Tycho.Events.Handling.IEventHandlingDispatcher, {{ source_class_name }}EventDispatcher>();
        }
    }
}
